{
  "name": "geotransport",
  "version": "1.2.0",
  "description": "GeoTransport — De Lijn GTFS-RT live map with cancellation support, served via Cloudflare Workers.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "secret": "wrangler secret put DL_GTFSRT"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "wrangler": "^3.0.0"
  }
}
