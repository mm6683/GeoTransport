{
  "name": "dl-gtfs-rt-map",
  "version": "1.1.0",
  "description": "De Lijn GTFS-RT realtime vehicle map — pure-JS protobuf decoder, no runtime deps.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "secret": "wrangler secret put DL_GTFSRT"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "wrangler": "^3.0.0"
  }
}
