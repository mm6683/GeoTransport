{
  "name": "dl-gtfs-rt-map",
  "version": "1.0.0",
  "description": "De Lijn GTFS-RT realtime vehicle map on a dark OSM layer, served via Cloudflare Workers.",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "secret": "wrangler secret put DL_GTFSRT"
  },
  "dependencies": {
    "gtfs-realtime-bindings": "^1.0.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0.0",
    "wrangler": "^3.0.0"
  }
}
