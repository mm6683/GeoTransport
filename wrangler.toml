name = "geotransport"
main = "src/worker.js"
compatibility_date = "2025-01-01"
compatibility_flags = ["nodejs_compat"]

assets = { directory = "./public", binding = "ASSETS" }

# Custom domain — requires geotransport.be to be on Cloudflare (proxied)
[[routes]]
pattern = "geotransport.be/*"
zone_name = "geotransport.be"

# Set the API key secret via CLI:
#   wrangler secret put DL_GTFSRT
# Then paste your Ocp-Apim-Subscription-Key when prompted.
